var React         = require( 'react' )
;

// depends Header, Nav
var Home = React.createClass({
  render: function() {
    return (
      <span style={{ display: 'none' }} />
    );
  }
});

module.exports = Home; 
